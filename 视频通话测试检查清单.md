# 视频通话功能验证检查清单

## 数据库验证

```sql
-- 1. 检查视频通话表是否存在
USE soulstation;
SHOW TABLES LIKE 'video_call%';

-- 2. 检查表结构
DESC video_call_sessions;
DESC video_call_events;

-- 3. 查询通话记录
SELECT * FROM video_call_sessions ORDER BY created_at DESC LIMIT 5;
SELECT * FROM video_call_events ORDER BY created_at DESC LIMIT 5;
```

## API 端点验证

### 1. WebRTC 配置 API
```bash
curl http://localhost:8000/api/video-call/config
```
**预期结果**：返回 ICE 服务器配置和媒体约束

### 2. 发起通话 API（需要 token）
```bash
curl -X POST "http://localhost:8000/api/video-call/call/initiate" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"appointment_id": 1, "call_type": "video"}'
```
**预期结果**：返回 session_id, room_id, ws_token 等

### 3. 通话状态 API
```bash
curl "http://localhost:8000/api/video-call/call/1/status" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

## 前端验证

### 1. 浏览器控制台检查
打开开发者工具（F12），检查：
- ✓ 无 JavaScript 错误
- ✓ WebSocket 连接成功（ws://localhost:8000/api/video-call/ws/video-call/...）
- ✓ WebRTC 相关日志无错误

### 2. 网络请求检查
在 Network 标签中：
- ✓ `/api/video-call/config` 返回 200
- ✓ `/api/video-call/call/initiate` 返回 200
- ✓ WebSocket 连接状态为 101 (Switching Protocols)

### 3. 媒体流检查
- ✓ 本地摄像头指示灯亮起
- ✓ 浏览器地址栏显示摄像头图标
- ✓ 在 `<video>` 元素中可以看到本地视频流

## 功能测试

### 基础功能
- [ ] 用户可以发起视频通话
- [ ] 用户可以发起语音通话
- [ ] 咨询师可以接听通话
- [ ] 双方可以看到/听到对方
- [ ] 通话时长正确计时
- [ ] 可以正常结束通话

### 通话控制
- [ ] 静音功能正常
- [ ] 关闭视频功能正常（仅视频通话）
- [ ] 挂断功能正常
- [ ] 对方挂断后收到通知

### 错误处理
- [ ] 无摄像头/麦克风权限时显示友好提示
- [ ] 对方拒绝时显示相应提示
- [ ] 网络断开时显示错误信息
- [ ] 超时未接听自动取消

## 性能检查

- [ ] 视频质量清晰（720p）
- [ ] 音频清晰无杂音
- [ ] 延迟低于 500ms
- [ ] 无明显的卡顿
- [ ] CPU 占用合理（< 50%）

## 兼容性测试

- [ ] Chrome 浏览器测试通过
- [ ] Firefox 浏览器测试通过
- [ ] Edge 浏览器测试通过
- [ ] 不同设备间通话测试通过
