# 多标签页登录冲突修复说明

## 问题描述

用户在同一浏览器的两个标签页分别登录不同账号时，先登录的账号会被强制登出，跳转到登录页面。

### 问题原因

1. **localStorage 共享问题**
   - localStorage 在同一浏览器的所有标签页之间共享
   - 多个标签页登录不同账号时，token 互相覆盖
   - 导致标签页A携带标签页B的 token 访问接口，权限不匹配

2. **Token 混用导致的权限错误**
   - 标签页A（用户账号）登录 → localStorage 存储 `token_user`
   - 标签页B（管理员账号）登录 → localStorage 被覆盖为 `token_admin`
   - 标签页A 发起 API 请求时携带管理员的 token
   - 后端返回 401 或权限错误
   - 前端拦截器检测到 401，清除 token 并跳转登录页

## 解决方案

采用 **sessionStorage（标签页隔离）+ localStorage（持久化）** 的混合存储策略：

### 核心思路

- **sessionStorage**: 每个标签页独立存储，实现标签页隔离
- **localStorage**: 跨标签页共享，保持数据持久化
- **读取顺序**: 优先从 sessionStorage 读取，其次从 localStorage 读取
- **写入策略**: 同时写入 sessionStorage 和 localStorage

### 实现细节

#### 1. 创建通用存储工具（frontend/src/utils/storage.js）

```javascript
// 优先从 sessionStorage 读取
export function getStorage(key, defaultValue = '') {
  const sessionValue = sessionStorage.getItem(key)
  if (sessionValue !== null) {
    return sessionValue
  }
  return localStorage.getItem(key) || defaultValue
}

// 同时写入 sessionStorage 和 localStorage
export function setStorage(key, value) {
  sessionStorage.setItem(key, value)
  localStorage.setItem(key, value)
}

// 同时清除 sessionStorage 和 localStorage
export function removeStorage(key) {
  sessionStorage.removeItem(key)
  localStorage.removeItem(key)
}
```

#### 2. 更新用户状态管理（frontend/src/stores/user.js）

- 使用 `getStorage()` 替代 `localStorage.getItem()`
- 使用 `setStorage()` 替代 `localStorage.setItem()`
- 使用 `removeStorage()` 替代 `localStorage.removeItem()`

#### 3. 更新 API 拦截器（frontend/src/api/index.js）

- 请求拦截器：优先从 sessionStorage 读取 token
- 响应拦截器：同时清除 sessionStorage 和 localStorage

#### 4. 更新路由守卫（frontend/src/router/index.js）

- 优先从 sessionStorage 读取 token 和 userRole

#### 5. 更新其他组件

- ChatIndex.vue
- VoiceRecorder.vue
- CounselorList.vue
- AdminLayout.vue

所有直接使用 `localStorage.getItem('token')` 的地方都改为使用 `getToken()` 工具函数。

## 修改的文件

### 新增文件

- `frontend/src/utils/storage.js` - 存储工具函数

### 修改文件

1. `frontend/src/stores/user.js` - 用户状态管理
2. `frontend/src/api/index.js` - API 请求拦截器
3. `frontend/src/api/chat.js` - 聊天 API
4. `frontend/src/router/index.js` - 路由守卫
5. `frontend/src/views/chat/ChatIndex.vue` - 聊天页面
6. `frontend/src/components/VoiceRecorder.vue` - 语音录制组件
7. `frontend/src/views/counselor/CounselorList.vue` - 咨询师列表
8. `frontend/src/layouts/AdminLayout.vue` - 管理员布局

## 测试验证

### 测试步骤

1. 打开浏览器标签页A，登录用户账号
2. 打开浏览器标签页B，登录管理员账号
3. 返回标签页A，操作页面（不会跳转到登录页）
4. 返回标签页B，操作页面（正常访问）
5. 关闭标签页A，重新打开，需要重新登录（sessionStorage 特性）

### 预期结果

- ✅ 两个标签页可以同时登录不同账号
- ✅ 每个标签页保持独立的登录状态
- ✅ 不会互相干扰导致登出
- ⚠️ 关闭标签页后需要重新登录（sessionStorage 特性，符合预期）

## 技术要点

### sessionStorage vs localStorage

| 特性 | sessionStorage | localStorage |
|------|---------------|--------------|
| 作用域 | 当前标签页 | 同源所有标签页 |
| 生命周期 | 关闭标签页清除 | 永久保存（除非手动清除） |
| 共享性 | 不共享 | 共享 |
| 容量 | 5-10MB | 5-10MB |

### 为什么采用混合策略？

1. **标签页隔离**: 使用 sessionStorage 避免多标签页登录冲突
2. **持久化**: 使用 localStorage 保持数据，刷新页面不丢失
3. **兼容性**: 优先读取 sessionStorage，降级到 localStorage

## 注意事项

1. **关闭标签页需要重新登录**
   - 这是 sessionStorage 的特性，符合安全要求
   - 如需保持登录，可以延长 token 有效期

2. **隐身模式/无痕模式**
   - 某些浏览器在隐身模式下可能限制 sessionStorage
   - 已做异常处理，降级到默认值

3. **存储空间限制**
   - sessionStorage 和 localStorage 都有容量限制（5-10MB）
   - 避免存储大量数据

## 后续优化建议

1. **Token 自动刷新**
   - 实现 Token 自动刷新机制
   - 避免频繁重新登录

2. **多设备登录管理**
   - 后端实现设备管理
   - 支持查看和移除其他设备

3. **单点登录（SSO）**
   - 如果需要单点登录，需要后端支持
   - 前端配合使用 localStorage 而非 sessionStorage

## 总结

通过使用 sessionStorage 实现标签页隔离，成功解决了多标签页登录冲突问题。用户现在可以在不同标签页同时登录不同账号，不会互相干扰。
